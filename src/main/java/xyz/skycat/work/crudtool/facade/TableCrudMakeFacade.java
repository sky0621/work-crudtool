package xyz.skycat.work.crudtool.facade;

import xyz.skycat.work.crudtool.facade.sqlparser.IfSqlParser;
import xyz.skycat.work.crudtool.old.old.old.parser.resolver.IfStatementResolver;

/**
 * Created by SS on 2016/06/10.
 */
public class TableCrudMakeFacade implements IfCrudMakeFacade {

    // [Define]
    // + use SQL parser
    // + use SQL statement resolver
    // + output way ( Console ? or CSV ? or TSV ? or ... )

    private IfSqlParser sqlParser;

    private IfStatementResolver statementResolver;

    // [Behave]
    // 1. create SQL parser
    // 2.

}
